const {
  Contact,
  joiContactsShema,
  updateFavoriteSchema,
} = require("./contact");

const { User, shemas } = require("./user");

module.exports = {
  Contact,
  joiContactsShema,
  updateFavoriteSchema,
  User,
  shemas,
};
